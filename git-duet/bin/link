#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)"
CONFIG_DIR="$HOME/.config/git-duet"

mkdir -p "$CONFIG_DIR"
rm -f "$CONFIG_DIR/authors.yml"
ln -s "$SCRIPT_DIR/../authors.yml" "$CONFIG_DIR/authors.yml"
