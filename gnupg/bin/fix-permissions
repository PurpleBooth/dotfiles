#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)"

GNU_DIR="$XDG_DATA_HOME/gnupg"
CONFIG_FILE="$GNU_DIR/gpg-agent.conf"

mkdir -p "$GNU_DIR"
chmod 700 "$GNU_DIR"
chmod 600 "$CONFIG_FILE"
