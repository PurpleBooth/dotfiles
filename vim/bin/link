#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)"

mkdir -p "$XDG_CONFIG_HOME/vim"
rm -f "$XDG_CONFIG_HOME/vim/.vimrc"
ln -s "$SCRIPT_DIR/../.vimrc" "$XDG_CONFIG_HOME/vim/.vimrc"
